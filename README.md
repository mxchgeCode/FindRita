# YOLO Cats Detector

Скрипт для обнаружения кошек на изображениях с использованием YOLOv11.

## Возможности

- Сканирует каталог `dataset` и находит все файлы с расширением `.jpg`
- Обрабатывает каждое изображение с помощью YOLOv11
- Сохраняет результаты в каталог `result`
- Создаёт каталог `result`, если он не существует
- Обрабатывает ошибки доступа к файлам
- Выводит информацию о ходе обработки с указанием имени файла и статуса
- Корректно завершается при возникновении исключений
- Автоматически очищает временные файлы

## Требования

- Python 3.8+
- ultralytics>=8.4.14
- pillow>=10.0.0
- numpy>=1.24.0

## Установка

```bash
pip install -r requirements.txt
```

Также необходимо скачать модель `yolo11n.pt` (скрипт загрузит её автоматически при первом запуске).

## Использование

```bash
python yolo_cats.py
```

## Структура проекта

```
RitaProject/
├── yolo_cats.py      # Основной скрипт
├── yolo11n.pt        # Модель YOLOv11 (загружается автоматически)
├── requirements.txt  # Зависимости
├── README.md         # Документация
├── dataset/          # Входные изображения
│   ├── cat1.jpg
│   ├── cat2.jpg
│   └── ...
└── result/           # Обработанные изображения
    ├── cat1.jpg
    ├── cat2.jpg
    └── ...
```

## Параметры

- Источник изображений: `dataset/`
- Выходной каталог: `result/`
- Порог уверенности: `0.25`

## Пример вывода

```
📁 Каталог result готов: C:\Users\adm\PycharmProjects\RitaProject\result
🗑️  Каталог result очищен
🔍 Найдено 4 изображений для обработки
--------------------------------------------------
🐱 Загружаю YOLOv11n...
✅ Модель загружена успешно

▶️  Обрабатываю: cat1.jpg
   ✅ Сохранено: cat1.jpg
   ✅ Успешно обработано: cat1.jpg
...
--------------------------------------------------
📊 Обработка завершена:
   ✅ Успешно: 4
   📁 Результаты сохранены в: C:\Users\adm\PycharmProjects\RitaProject\result
```
